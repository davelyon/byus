- url = link.location
%li{ class: cycle('bin_link_dark','bin_link_light') }
  = link_to truncate(url, length: 60), url
  - unless bin.nil?
    = link_to "[delete]", bin_link_path(bin.secret_hash,link.id), method: :delete, class: 'delete'
